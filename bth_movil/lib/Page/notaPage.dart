import 'package:flutter/material.dart';

class NotaPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Bueno esto deve ser la nota"),
    );
  }
}